{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"gpu","dataSources":[{"sourceType":"datasetVersion","sourceId":4558742,"datasetId":2660745,"databundleVersionId":4619797}],"dockerImageVersionId":31260,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":true}},"nbformat_minor":4,"nbformat":4,"cells":[{"source":"<a href=\"https://www.kaggle.com/code/niladriroy0/modular-transformer-research-framework?scriptVersionId=297999430\" target=\"_blank\"><img align=\"left\" alt=\"Kaggle\" title=\"Open in Kaggle\" src=\"https://kaggle.com/static/images/open-in-kaggle.svg\"></a>","metadata":{},"cell_type":"markdown"},{"cell_type":"code","source":"# ======================================================\n# RESEARCH-GRADE TRANSFORMER FRAMEWORK (FULL LOGGING)\n# Optimized for Kaggle with Causal Masking & Memory Efficiency\n# ======================================================\n\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nimport pandas as pd\nimport numpy as np\nimport random\nimport time\nimport math\nfrom itertools import product\nfrom tqdm.auto import tqdm\n\nprint(\"\\n================ INITIALIZING FRAMEWORK ================\")\n\n# ======================================================\n# 1. REPRODUCIBILITY\n# ======================================================\n\ndef set_seed(seed: int):\n    random.seed(seed)\n    np.random.seed(seed)\n    torch.manual_seed(seed)\n    torch.cuda.manual_seed_all(seed)\n    torch.backends.cudnn.deterministic = True\n    torch.backends.cudnn.benchmark = False\n\n# ======================================================\n# 2. CONFIG\n# ======================================================\n\nclass Config:\n    def __init__(self, **kwargs):\n        self.d_model = 128\n        self.num_heads = 8\n        self.num_layers = 2\n        self.seq_len = 128\n        self.batch_size = 64\n        self.lr = 3e-4\n        self.epochs = 10\n        self.dropout = 0.1\n        self.weight_decay = 0.01\n        self.grad_clip = 1.0\n        self.device = \"cuda\" if torch.cuda.is_available() else \"cpu\"\n        \n        # Grid parameters\n        self.attention_type = \"standard\"\n        self.pos_encoding = \"learned\"\n        self.activation = \"relu\"\n        self.ff_multiplier = 4\n\n        for k, v in kwargs.items():\n            setattr(self, k, v)\n\n# ======================================================\n# 3. TOKENIZER & DATASET (Slicing Optimized)\n# ======================================================\n\nclass CharTokenizer:\n    def __init__(self, text):\n        print(\"[Tokenizer] Building vocabulary...\")\n        self.chars = sorted(list(set(text)))\n        self.vocab_size = len(self.chars)\n        print(f\"[Tokenizer] Vocabulary size: {self.vocab_size}\")\n        self.stoi = {ch: i for i, ch in enumerate(self.chars)}\n        self.itos = {i: ch for i, ch in enumerate(self.chars)}\n\n    def encode(self, text):\n        return [self.stoi[ch] for ch in text if ch in self.stoi]\n\n\nclass TextDataset(torch.utils.data.Dataset):\n    def __init__(self, text, tokenizer, seq_len):\n        encoded = tokenizer.encode(text)\n        self.data = torch.tensor(encoded, dtype=torch.long)\n        self.seq_len = seq_len\n        self.num_samples = (len(self.data) - 1) // seq_len\n        print(f\"[Dataset] Total samples available: {self.num_samples}\")\n\n    def __len__(self):\n        return self.num_samples\n\n    def __getitem__(self, idx):\n        start = idx * self.seq_len\n        end = start + self.seq_len\n        x = self.data[start:end]\n        y = self.data[start+1:end+1]\n        return x, y\n\n# ======================================================\n# 4. MODEL (Causal Masking & Logic Switch)\n# ======================================================\n\nclass TransformerModel(nn.Module):\n    def __init__(self, config, vocab_size):\n        super().__init__()\n        self.config = config\n        \n        self.embedding = nn.Embedding(vocab_size, config.d_model)\n        \n        if config.pos_encoding == \"learned\":\n            self.pos_embedding = nn.Embedding(config.seq_len, config.d_model)\n        else:\n            self.pos_embedding = nn.Parameter(torch.zeros(1, config.seq_len, config.d_model))\n\n        encoder_layer = nn.TransformerEncoderLayer(\n            d_model=config.d_model,\n            nhead=config.num_heads,\n            dim_feedforward=config.d_model * config.ff_multiplier,\n            dropout=config.dropout,\n            activation=config.activation,\n            batch_first=True\n        )\n        self.transformer = nn.TransformerEncoder(encoder_layer, config.num_layers)\n\n        self.norm = nn.LayerNorm(config.d_model)\n        self.output = nn.Linear(config.d_model, vocab_size)\n\n    def forward(self, x):\n        sz = x.size(1)\n        mask = torch.triu(torch.ones(sz, sz, device=x.device) * float('-inf'), diagonal=1)\n        positions = torch.arange(0, sz, device=x.device).unsqueeze(0)\n        x = (self.embedding(x) * math.sqrt(self.config.d_model)) + self.pos_embedding(positions)\n        x = self.transformer(x, mask=mask)\n        x = self.norm(x)\n        return self.output(x)\n\n# ======================================================\n# 5. TRAINING + FULL LOGGING\n# ======================================================\n\ndef evaluate(model, loader, config):\n    model.eval()\n    criterion = nn.CrossEntropyLoss()\n    total_loss = 0\n    with torch.no_grad():\n        for x, y in loader:\n            x, y = x.to(config.device), y.to(config.device)\n            logits = model(x)\n            loss = criterion(logits.view(-1, logits.size(-1)), y.view(-1))\n            total_loss += loss.item()\n    avg_loss = total_loss / len(loader) if len(loader) > 0 else 0\n    return avg_loss, math.exp(min(avg_loss, 20))\n\n\ndef train_model(model, train_loader, val_loader, config, exp_id, seed):\n    model.to(config.device)\n    optimizer = optim.AdamW(model.parameters(), lr=config.lr, weight_decay=config.weight_decay)\n    scheduler = optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=config.epochs)\n    criterion = nn.CrossEntropyLoss()\n\n    epoch_logs = []\n    batch_logs = []\n    start_time = time.time()\n\n    for epoch in range(config.epochs):\n        model.train()\n        running_loss = 0\n        pbar = tqdm(enumerate(train_loader), total=len(train_loader), desc=f\"Exp {exp_id} Seed {seed} Ep {epoch+1}\", leave=False)\n        \n        for batch_idx, (x, y) in pbar:\n            x, y = x.to(config.device), y.to(config.device)\n            optimizer.zero_grad()\n            logits = model(x)\n            loss = criterion(logits.view(-1, logits.size(-1)), y.view(-1))\n            loss.backward()\n            torch.nn.utils.clip_grad_norm_(model.parameters(), config.grad_clip)\n            optimizer.step()\n            running_loss += loss.item()\n\n            batch_logs.append({\n                \"level\": \"batch\", \"experiment\": exp_id, \"seed\": seed,\n                \"epoch\": epoch+1, \"batch\": batch_idx, \"train_loss\": loss.item(),\n                \"val_loss\": None, \"val_perplexity\": None,\n                \"learning_rate\": optimizer.param_groups[0]['lr'], \"train_time\": None,\n                \"activation\": config.activation,\n                \"dropout\": config.dropout,\n                \"lr_config\": config.lr,\n                \"ff_multiplier\": config.ff_multiplier\n            })\n            \n            if batch_idx % 10 == 0:\n                pbar.set_postfix(loss=f\"{loss.item():.4f}\")\n\n        scheduler.step()\n        train_loss = running_loss / len(train_loader) if len(train_loader) > 0 else 0\n        val_loss, val_ppl = evaluate(model, val_loader, config)\n\n        epoch_logs.append({\n            \"level\": \"epoch\", \"experiment\": exp_id, \"seed\": seed,\n            \"epoch\": epoch+1, \"batch\": None, \"train_loss\": train_loss,\n            \"val_loss\": val_loss, \"val_perplexity\": val_ppl,\n            \"learning_rate\": optimizer.param_groups[0]['lr'], \"train_time\": None,\n            \"activation\": config.activation,\n            \"dropout\": config.dropout,\n            \"lr_config\": config.lr,\n            \"ff_multiplier\": config.ff_multiplier\n        })\n        print(f\"[Experiment {exp_id}, Seed {seed}, Epoch {epoch+1}] Train Loss: {train_loss:.4f} | Val Loss: {val_loss:.4f} | PPL: {val_ppl:.2f} 🤯\")\n\n    print(\"-------------------------------------------------------------------------------\")\n    total_time = time.time() - start_time\n    for row in epoch_logs:\n        row[\"train_time\"] = total_time\n    return epoch_logs, batch_logs\n\n# ======================================================\n# 6. DATA LOADING\n# ======================================================\n\nprint(\"\\n================ LOADING DATA ================\")\nDATA_PATH = \"/kaggle/input/datasets/thedevastator/the-bards-best-a-character-modeling-dataset\"\n\ntry:\n    train_df = pd.read_csv(f\"{DATA_PATH}/train.csv\")\n    val_df = pd.read_csv(f\"{DATA_PATH}/validation.csv\")\n    train_text = \"\\n\".join(train_df[\"text\"].astype(str).tolist())\n    val_text = \"\\n\".join(val_df[\"text\"].astype(str).tolist())\n    print(f\"Train rows: {len(train_df)} | Val rows: {len(val_df)}\")\nexcept Exception as e:\n    print(f\"Data loading error: {e}. Using dummy data.\")\n    train_text = \"to be or not to be that is the question \" * 1000\n    val_text = \"shall i compare thee to a summers day \" * 200\n\ntokenizer = CharTokenizer(train_text)\ntrain_dataset = TextDataset(train_text, tokenizer, seq_len=128)\nval_dataset = TextDataset(val_text, tokenizer, seq_len=128)\ntrain_loader = torch.utils.data.DataLoader(train_dataset, batch_size=64, shuffle=True, drop_last=True)\nval_loader = torch.utils.data.DataLoader(val_dataset, batch_size=64, shuffle=False, drop_last=True)\n\n# ======================================================\n# 7. RUN EXPERIMENTS\n# ======================================================\n\nSEEDS = [42, 999]\n\ngrid_params = {\n    \"attention_type\": [\"standard\"],\n    \"pos_encoding\": [\"learned\"],\n    # --- DEPTH SCALING ---\n    \"num_layers\": [4, 6],\n\n    # --- WIDTH SCALING ---\n    \"d_model\": [124, 248],\n\n    # --- HEAD VARIATION (Keep divisible with d_model) ---\n    \"num_heads\": [4, 8],\n\n    # --- ACTIVATION ---\n    \"activation\": [\"relu\", \"gelu\"],\n\n    # --- REGULARIZATION ---\n    \"dropout\": [0.1],\n\n    # --- LEARNING RATE STABILITY ---\n    \"lr\": [3e-4],\n\n    # --- FF CAPACITY ---\n    \"ff_multiplier\": [8]\n}\n\nkeys, values = zip(*grid_params.items())\nexperiment_grid = [dict(zip(keys, v)) for v in product(*values)]\n\nprint(f\"[Grid] Total unique configurations: {len(experiment_grid)}\")\nprint(f\"[Grid] Total runs (Configs x Seeds): {len(experiment_grid) * len(SEEDS)}\")\n\nMASTER_LOGS = []\n\nfor exp_id, exp in enumerate(experiment_grid, 1):\n    print(f\"\\n>>>> Starting Experiment {exp_id}/{len(experiment_grid)} | Config: {exp}\")\n    for seed in SEEDS:\n        set_seed(seed)\n        config = Config(**exp)\n        if torch.cuda.is_available():\n            torch.cuda.empty_cache()\n\n        model = TransformerModel(config, vocab_size=tokenizer.vocab_size)\n        epoch_logs, batch_logs = train_model(model, train_loader, val_loader, config, exp_id, seed)\n        MASTER_LOGS.extend(epoch_logs)\n        MASTER_LOGS.extend(batch_logs)\n\nprint(\"\\n================ ALL EXPERIMENTS COMPLETED ================\")\nFINAL_DF = pd.DataFrame(MASTER_LOGS)\nFINAL_DF.to_csv(\"full_experiment_log.csv\", index=False)\n\nprint(f\"\\n===== LOGGING COMPLETE =====\")\nprint(FINAL_DF.groupby(['experiment', 'seed']).tail(1)[['experiment', 'seed', 'val_loss', 'val_perplexity']])\n\n# ======================================================\n# 8. RESEARCH-STYLE SUMMARY (Mean ± Std Across Seeds)\n# ======================================================\n\nsummary = FINAL_DF[FINAL_DF[\"level\"] == \"epoch\"].groupby([\"experiment\"]).agg({\"val_loss\": [\"mean\", \"std\"],\"val_perplexity\": [\"mean\", \"std\"]})\n\nprint(\"\\n===== MEAN ± STD SUMMARY ACROSS SEEDS =====\")\nprint(summary)\n","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true,"execution":{"iopub.status.busy":"2026-02-16T09:30:18.529534Z","iopub.execute_input":"2026-02-16T09:30:18.5298Z","execution_failed":"2026-02-16T09:30:54.104Z"}},"outputs":[{"name":"stdout","text":"\n================ INITIALIZING FRAMEWORK ================\n\n================ LOADING DATA ================\nTrain rows: 1 | Val rows: 1\n[Tokenizer] Building vocabulary...\n[Tokenizer] Vocabulary size: 65\n[Dataset] Total samples available: 7842\n[Dataset] Total samples available: 435\n[Grid] Total unique configurations: 16\n[Grid] Total runs (Configs x Seeds): 32\n\n>>>> Starting Experiment 1/16 | Config: {'attention_type': 'standard', 'pos_encoding': 'learned', 'num_layers': 4, 'd_model': 124, 'num_heads': 4, 'activation': 'relu', 'dropout': 0.1, 'lr': 0.0003, 'ff_multiplier': 8}\n","output_type":"stream"},{"output_type":"display_data","data":{"text/plain":"Exp 1 Seed 42 Ep 1:   0%|          | 0/122 [00:00<?, ?it/s]","application/vnd.jupyter.widget-view+json":{"version_major":2,"version_minor":0,"model_id":"ee5192abdb5b46479dbf26bd56db49c8"}},"metadata":{}},{"name":"stdout","text":"[Experiment 1, Seed 42, Epoch 1] Train Loss: 2.7475 | Val Loss: 2.5027 | PPL: 12.21 🤯\n","output_type":"stream"},{"output_type":"display_data","data":{"text/plain":"Exp 1 Seed 42 Ep 2:   0%|          | 0/122 [00:00<?, ?it/s]","application/vnd.jupyter.widget-view+json":{"version_major":2,"version_minor":0,"model_id":"e9f0d6904e86416b86e7aa6d85a0b380"}},"metadata":{}},{"name":"stdout","text":"[Experiment 1, Seed 42, Epoch 2] Train Loss: 2.4734 | Val Loss: 2.4405 | PPL: 11.48 🤯\n","output_type":"stream"},{"output_type":"display_data","data":{"text/plain":"Exp 1 Seed 42 Ep 3:   0%|          | 0/122 [00:00<?, ?it/s]","application/vnd.jupyter.widget-view+json":{"version_major":2,"version_minor":0,"model_id":"ed8e801865674fb598e3ab5a9db1b213"}},"metadata":{}},{"name":"stdout","text":"[Experiment 1, Seed 42, Epoch 3] Train Loss: 2.4143 | Val Loss: 2.3912 | PPL: 10.93 🤯\n","output_type":"stream"},{"output_type":"display_data","data":{"text/plain":"Exp 1 Seed 42 Ep 4:   0%|          | 0/122 [00:00<?, ?it/s]","application/vnd.jupyter.widget-view+json":{"version_major":2,"version_minor":0,"model_id":"ea7e0d1d208240598e02b5d313545ab0"}},"metadata":{}},{"name":"stdout","text":"[Experiment 1, Seed 42, Epoch 4] Train Loss: 2.3725 | Val Loss: 2.3394 | PPL: 10.38 🤯\n","output_type":"stream"},{"output_type":"display_data","data":{"text/plain":"Exp 1 Seed 42 Ep 5:   0%|          | 0/122 [00:00<?, ?it/s]","application/vnd.jupyter.widget-view+json":{"version_major":2,"version_minor":0,"model_id":"6472ad5adbe24014828efbeb0b21a062"}},"metadata":{}},{"name":"stdout","text":"[Experiment 1, Seed 42, Epoch 5] Train Loss: 2.3316 | Val Loss: 2.2941 | PPL: 9.92 🤯\n","output_type":"stream"},{"output_type":"display_data","data":{"text/plain":"Exp 1 Seed 42 Ep 6:   0%|          | 0/122 [00:00<?, ?it/s]","application/vnd.jupyter.widget-view+json":{"version_major":2,"version_minor":0,"model_id":"c1ed87b30aa4491290d6f5a040efa3d4"}},"metadata":{}},{"name":"stdout","text":"[Experiment 1, Seed 42, Epoch 6] Train Loss: 2.2918 | Val Loss: 2.2499 | PPL: 9.49 🤯\n","output_type":"stream"},{"output_type":"display_data","data":{"text/plain":"Exp 1 Seed 42 Ep 7:   0%|          | 0/122 [00:00<?, ?it/s]","application/vnd.jupyter.widget-view+json":{"version_major":2,"version_minor":0,"model_id":"5607f235dad94843bb17726cffd47637"}},"metadata":{}},{"name":"stdout","text":"[Experiment 1, Seed 42, Epoch 7] Train Loss: 2.2604 | Val Loss: 2.2247 | PPL: 9.25 🤯\n","output_type":"stream"},{"output_type":"display_data","data":{"text/plain":"Exp 1 Seed 42 Ep 8:   0%|          | 0/122 [00:00<?, ?it/s]","application/vnd.jupyter.widget-view+json":{"version_major":2,"version_minor":0,"model_id":"18e99cf00d41440185f7c8c719547873"}},"metadata":{}}],"execution_count":null},{"cell_type":"code","source":"FINAL_DF.head(50)","metadata":{"trusted":true,"execution":{"execution_failed":"2026-02-16T09:30:54.104Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"FINAL_DF.info()","metadata":{"trusted":true,"execution":{"execution_failed":"2026-02-16T09:30:54.104Z"}},"outputs":[],"execution_count":null}]}